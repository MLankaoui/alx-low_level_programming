#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	alpha[] = "abcdfghijklmnoprstuvwxyz";

	for(i = 0;alpha[i]i++)
		putchar(alpha[i])
	putchar("\n");
	return (0);
}
