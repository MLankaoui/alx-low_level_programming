#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n, N;

	n = 01234;
	N = 56789;
	int i;

	for (i = 0 ; i < 5 ; i++)
	{
		putchar(n + 'O');
	}
	for (i = 0 ; i < 5 ; i++)
	{
		putchar(n + 'O');
	}
	putchar('\n');
	return (0);
}
